# ── Maven build outputs ────────────────────────────────────────────────────────
**/target/
target/

# ── Maven wrapper (check in mvnw, not the binary cache) ───────────────────────
maven/
maven.zip

# ── IDE files ─────────────────────────────────────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.swp
*.swo
*.orig

# ── Compiled class files ──────────────────────────────────────────────────────
*.class

# ── Secrets and environment files ─────────────────────────────────────────────
.env
.env.*
!.env.example
**/application-local.yml
**/application-secret.yml
**/application-prod.yml

# ── IBKR TWS API JAR (must be obtained from IBKR website, see libs/README.md) ─
libs/TwsApi*.jar

# ── Logs ──────────────────────────────────────────────────────────────────────
logs/
*.log
*.log.*

# ── OS artifacts ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Gradle / IDE build directories ────────────────────────────────────────────
# (project uses Maven for CI, but local IDE/Gradle runs create these)
**/build/
build/


# ── Docker volumes ────────────────────────────────────────────────────────────
docker/volumes/
docker/data/

# ── Temp / scratch ────────────────────────────────────────────────────────────
build-errors.txt
RepairFlyway.*
*.tmp

# ── Node (frontend) ───────────────────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.angular/
